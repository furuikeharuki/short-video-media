# ===== 環境変数（絶対にコミットしない） =====
.env
.env.local
.env.production      # ← 追加：本番用も念のため除外
.env.*.local         # ← 追加：.env.development.local 等もカバー

# ただしこれはコミットする（テンプレートなので）
# !.env.example       ← .gitignoreに書かなくても上記に引っかからないのでOK

# ===== Python =====
*.pyc
__pycache__/         # ← 追加
.venv/               # ← 追加：仮想環境
*.egg-info/          # ← 追加

# ===== Node.js =====
apps/web/node_modules/*
# ルート直下にうっかり生成された依存関係も除外
/node_modules/
/package-lock.json
apps/web/.next/
apps/web/out/        # ← 追加

# ===== OS / エディタ =====
.DS_Store            # ← 追加