# Browser-safe values
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_VERCEL_ENV=development

# 広告 (ExoClick) — "true" にすると /feed 上部にバナーを表示
NEXT_PUBLIC_ADS_ENABLED=false
# 任意: ExoClick zone id を上書きする場合のみ。未指定なら 5929876。
NEXT_PUBLIC_EXOCLICK_BANNER_ZONE_ID=

# Server-only values
INTERNAL_API_TOKEN=